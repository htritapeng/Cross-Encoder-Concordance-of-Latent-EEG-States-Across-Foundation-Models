# Frozen model weights go here (not included in this repository by default).
#
# Expected (example):
#   models/luna/<checkpoint files for LUNA-Base>
#   models/our/<checkpoint files for the lightweight CNN-Transformer model>
#
# After you upload weights, document the exact filenames in this folder's README
# or in the top-level README "Frozen models" section.
#
# The occupancy concordance demo does NOT need these files.
